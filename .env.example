# MindMate AI — no environment variables are required.
#
# This build has no backend, no database, and no external API. Auth,
# assessments, and admin analytics all run client-side against the
# browser's localStorage (see src/services/mock*.js). There is nothing
# to configure — clone, `npm install`, `npm run dev`, and it works.
